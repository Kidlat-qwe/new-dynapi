# API gateway (api-backend). CMS is served at /api/cms on this origin.
VITE_API_ORIGIN=http://localhost:3000
# Required for gateway (Admin → API Tokens, system cms). Uncomment and set:
VITE_CMS_API_TOKEN=sk_4c377d21313cc0ce7f5561814faa03e88120d5fdbfd779a3a31e3c0c818d990b

# Legacy: if VITE_API_ORIGIN is unset, origin is derived from this URL
# VITE_API_BASE_URL=http://localhost:3000/api/cms

# Supabase (if your build still uses these — restore values from your backup)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=