# Frontend config

- **firebase.js** — Firebase client SDK (Auth). Uses `VITE_FIREBASE_*` env vars if set; otherwise uses built-in defaults for project `new-api-a316d`. For production you can set `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, etc. in `.env`.
